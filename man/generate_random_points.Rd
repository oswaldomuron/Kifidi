% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_random_points.R
\name{generate_random_points}
\alias{generate_random_points}
\title{Generate Random Sampling Points in a Plot}
\usage{
generate_random_points(
  plot_length = 3,
  plot_width = 3,
  n_points = 5,
  seed = NULL,
  export_csv = FALSE,
  filename = "random_coordinates.csv"
)
}
\arguments{
\item{plot_length}{Numeric. Length of the plot in meters. Default is 3.}

\item{plot_width}{Numeric. Width of the plot in meters. Default is 3.}

\item{n_points}{Integer. Number of random points to generate. Default is 5.}

\item{seed}{Integer or NULL. Seed for random number generator to reproduce results. Default is NULL (random every run).}

\item{export_csv}{Logical. Whether to export the coordinates as a CSV file. Default is FALSE.}

\item{filename}{Character. Name of the CSV file to export if \code{export_csv = TRUE}. Default is "random_coordinates.csv".}
}
\value{
A data.frame with columns: Point, X_meters, Y_meters.
}
\description{
This function generates random (x, y) coordinates within a rectangular plot area
for biomass or other sampling. It can plot the points and optionally export them as CSV.
}
\examples{
# Generate 5 random points in a 3x3 m plot, plot and export csv
generate_random_points(plot_length = 3, plot_width = 3, n_points = 5, seed = 42,
                       export_csv = TRUE, filename = "points.csv")

# Generate random points without fixed seed (different each run)
generate_random_points(n_points = 10)

}
